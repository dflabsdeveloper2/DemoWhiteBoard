<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:background="@android:color/transparent">

    <ImageView
        android:id="@+id/ivItemRotationMainImagen"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:background="@drawable/bg_rotation_item"
        android:src="@drawable/arrows_rotate"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="0.0" />

    <ImageView
        android:id="@+id/ivMainImage"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="30dp"
        android:padding="8dp"
        android:background="@drawable/bg_view_image_tool"
        android:scaleType="centerCrop"
        android:src="@drawable/backgroundimagepaisaje"
        app:layout_constraintBottom_toTopOf="@id/ivOkMainImage"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/ivItemRotationMainImagen" />

    <ImageView
        android:id="@+id/ivCloseMainImage"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:layout_margin="4dp"
        android:background="@drawable/bg_rotation_item"
        android:src="@drawable/close_rule"
        app:layout_constraintBottom_toBottomOf="@id/ivMainImage"
        app:layout_constraintEnd_toEndOf="@id/ivMainImage" />

    <ImageView
        android:id="@+id/ivOkMainImage"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:layout_margin="4dp"
        android:background="@drawable/bg_rotation_item"
        android:src="@drawable/ic_tick"
        app:layout_constraintBottom_toBottomOf="@id/ivMainImage"
        app:layout_constraintEnd_toStartOf="@id/ivCloseMainImage" />

</androidx.constraintlayout.widget.ConstraintLayout>